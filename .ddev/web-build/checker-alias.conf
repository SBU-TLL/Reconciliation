# DDEV-only stand-in for the internal "checker" project (login prompt).
Alias /checker /var/www/html/.ddev/shib/checker
# The stub lives outside the docroot (www/), so grant access explicitly.
<Directory /var/www/html/.ddev/shib/checker>
    Require all granted
    AllowOverride All
</Directory>
